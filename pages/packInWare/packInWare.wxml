<!--pages/packInWare/packInWare.wxml-->
<van-cell-group>
      <view wx:for="{{packList}}" wx:key="index">
<van-cell  title="{{item.hawbNo}}" value="{{item.createTime}}" label="{{item.goodsType}} " title-width="60%" data-id="{{item.id}}" bindtap="viewDetail"/>
      </view>
    </van-cell-group>
